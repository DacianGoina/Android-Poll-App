<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.deming">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Deming">
        <activity android:name=".WorkProgressActivity"></activity>
        <activity android:name=".ComputeResultsActivity" />
        <activity android:name=".AddRecordActivity" />
        <activity android:name=".ViewPollActivity" />
        <activity android:name=".CreatePoll3Activity" />
        <activity android:name=".CreatePoll2Activity" />
        <activity android:name=".CreatePollActivity" />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".UserMainActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SuccessRegistrationActivity" />
        <activity android:name=".CreateAccountActivity" />
        <activity
            android:name=".MainActivity2"
            android:label="@string/title_activity_main2"
            android:theme="@style/Theme.Deming.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>